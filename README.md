# ternary search tree
Using make:
```
make
```
Using cmake:
```
mkdir build && cd build
cmake .. && make
```

Fuzz:
```
python3 fuzz.py
```

Usage:
```
./project1.out "insert a, delete a, lookup a, rangesearch a to b"
```
